version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: depinsight:latest
    container_name: depinsight
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

